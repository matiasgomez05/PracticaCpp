#include<iostream>

using namespace std;
/*
int main()
{
    // El problema solo contempla los datos int y float. 
    // Deja de funcionar si se ingresa un dato de otro tipo (char, int o float donde no corresponde)
    
    int horasTrabajadas;
    float costoHora;    // Variable declarada tipo FLOAT (Numeros con coma)
    float sueldo;
    cout << "Ingrese Horas trabajadas por el operario: ";
    cin >> horasTrabajadas; // cin para leer un dato por consola
    cout << "Ingrese el pago por hora: ";
    cin >> costoHora;
    sueldo = horasTrabajadas * costoHora;
    cout << "El sueldo total del operario es: ";
    cout << sueldo;
    cin.get();  // El doble llamado de cin.get() evita que se cierre el programa antes de mostrar la variable sueldo
    cin.get();  // A su vez captura el enter para que el programa se cierre correctamente despues de mostrar el dato.
    return 0;
}
*/