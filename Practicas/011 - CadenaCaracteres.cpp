#include <iostream>

using namespace std;
/*
int main() {
    //  Cadenas de caracteres con Char; cada letra ocupa una posicion en el array seguido del terminador '\0'.
    //  Ej:
    //  Componente  [0] [1] [2] [3] [4]
    //  Cadena		'h' 'o' 'l' 'a' '\0'    

    // 1) Confeccionar un programa que defina una cadena de caracteres para almacenar el nombre y apellido de una persona. 
    // La carga del dato hacerla por teclado.

    char nombre[40];
    cout << "Ingrese su nombre: ";
    cin.getline(nombre, 40);
    cout << "El nombre ingresado es: ";
    cout << nombre;
    return 0;

    //  Comparar caracteres:
    //  int strcmp(cadena1,cadena2)
    //  Retorna 0 si las dos cadenas son exactamente iguales, 
    //  > a 0 si cadena1 es mayor alfabéticamente que cadena2 y: 
    //  < a 0 en caso contrario.


    char cadena1[50];
    char cadena2[50];
    cout << "Ingrese la primer cadena: ";
    cin.getline(cadena1, 50);
    cout << "Ingrese la segunda cadena: ";
    cin.getline(cadena2, 50);
    if (strcmp(cadena1, cadena2) == 0)
    {
        cout << "son iguales";
    }
    else
    {
        if (strcmp(cadena1, cadena2) > 0)
        {
            cout << "la primer cadena ingresada es mayor alfabeticamente";
        }
        else
        {
            cout << "la segunda cadena ingresada es mayor alfabéticamente";
        }
    }

	return 0;
}
*/